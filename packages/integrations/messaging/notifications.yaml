# Created by Danny Tsang <danny@tsang.uk>
# Integration: https://www.home-assistant.io/integrations/slack/
# Integration: https://www.home-assistant.io/integrations/mobile_app/
# Integration: https://www.home-assistant.io/integrations/telegram/
automation:
  - id: "1625924056779"
    alias: Mobile Notification Action Router
    description: ""
    trigger:
      - platform: event
        event_type: mobile_app_notification_action
        event_data: {}
    condition: []
    action:
      - choose:
          - conditions:
              - condition: template
                value_template:
                  "{{ trigger.event.data.action == 'set_bedroom_blinds_30'
                  }}"
              - condition: numeric_state
                entity_id: cover.bedroom_blinds
                attribute: current_position
                above: "30"
            sequence:
              - service: script.send_to_home_log
                data:
                  message: ":window: Closing bedroom blinds event received."
              - service: cover.set_cover_position
                target:
                  entity_id: cover.bedroom_blinds
                data:
                  position: 30
          - conditions:
              - condition: template
                value_template: "{{ trigger.event.data.action == 'server_fan_off' }}"
            sequence:
              - service: script.send_to_home_log
                data:
                  message: Turning off server fan event received.
              - service: switch.turn_off
                target:
                  entity_id: switch.server_fan
          - conditions:
              - condition: template
                value_template:
                  "{{ trigger.event.data.action == 'switch_on_office_fan'
                  }}"
            sequence:
              - service: script.send_to_home_log
                data:
                  message: Turning on office fan event received.
              - service: switch.turn_on
                target:
                  entity_id: switch.office_fan
          - conditions:
              - condition: template
                value_template:
                  "{{ trigger.event.data.action == 'switch_on_fridge_freezer'
                  }}"
            sequence:
              - service: script.send_to_home_log
                data:
                  message: Turning on fridge freezer event received.
              - service: switch.turn_on
                target:
                  entity_id: switch.ecoflow_kitchen_plug
          - conditions:
              - condition: template
                value_template: "{{ trigger.event.data.action == 'switch_on_freezer' }}"
            sequence:
              - service: script.send_to_home_log
                data:
                  message: Turning on freezer event received.
              - service: switch.turn_on
                target:
                  entity_id: switch.freezer
          - conditions:
              - condition: template
                value_template:
                  "{{ trigger.event.data.action == 'guest_mode_arm_alarm_and_turn_off_devices'
                  }}"
            sequence:
              - service: script.send_to_home_log
                data:
                  message: ":bell: Guest mode override. Turning alarm on and devices off."
              - service: script.set_alarm_to_away_mode
              - service: script.lock_front_door
              - service: script.everybody_leave_home
          - conditions:
              - condition: template
                value_template:
                  "{{ trigger.event.data.action == 'guest_mode_arm_alarm_away'
                  }}"
              - condition: state
                entity_id: input_select.home_mode
                state: "Guest"
            sequence:
              - service: script.send_to_home_log
                data:
                  message:
                    ":bell: Guest mode override. Turning alarm in away mode (motion
                    and sensors) on only."
              - service: script.set_alarm_to_away_mode
              - service: script.lock_front_door
          - conditions:
              - condition: template
                value_template:
                  "{{ trigger.event.data.action == 'guest_mode_turn_off_devices'
                  }}"
            sequence:
              - service: script.send_to_home_log
                data:
                  message: Guest mode override. Turning devices off only.
              - service: script.everybody_leave_home
          - conditions:
              - condition: template
                value_template: "{{ trigger.event.data.action == 'switch_off_alarm' }}"
            sequence:
              - service: script.send_to_home_log
                data:
                  message: Alarm override. Turning Alarm off.
              - service: script.set_alarm_to_disarmed_mode
          - conditions:
              - condition: template
                value_template: "{{ trigger.event.data.action == 'switch_off_attic_lights' }}"
            sequence:
              - service: script.send_to_home_log
                data:
                  message: Turning attic lights off event.
              - service: light.turn_off
                data: {}
                target:
                  entity_id: light.attic
          - conditions:
              - condition: template
                value_template: "{{ trigger.event.data.action == 'update_home_assistant' }}"
            sequence:
              - service: script.send_to_home_log
                data:
                  message: Upgrade Home Assistant event.
              - service: update.install
                data:
                  backup: true
                target:
                  entity_id: update.home_assistant_core_update
          - conditions:
              - condition: template
                value_template: "{{ trigger.event.data.action == 'zappi_stop' }}"
            sequence:
              - service: script.send_to_home_log
                data:
                  message: Stop Zappi event.
              - service: select.select_option
                data:
                  option: Stopped
                target:
                  entity_id: select.myenergi_zappi_charge_mode
        default: []
    mode: queued
    max: 10

input_text:
  # Need the Telegram chat id for telegram integration platform
  # so no point in storing it in a helper and secret.
  dannys_telegram_chat_id:
    name: Danny's Telegram Chat ID
    icon: mdi:message-reply-text
    initial: !secret telegram_danny_chat_id

notify:
  - platform: telegram
    name: telegram
    chat_id: !secret telegram_danny_chat_id

script:
  # Encapsulated scripts
  send_to_home_log:
    alias: Send To Home Log
    icon: mdi:message-outline
    description: "Post a text message to the home_log channel."
    # https://github.com/home-assistant/frontend/issues/8591
    fields:
      message:
        description: Message to post.
        required: true
        selector:
          text:
            multiline: true
      title:
        description: (optional)Title to the message posted.
        selector:
          text:
      log_level:
        description: Level of logging. If it matches or high level, then it will be sent.
        example: >-
          If logging is set to *Normal* and the current logging level is *Debug*, then it
          will be sent to the chosen home log platforms.
        default: "Debug"
        selector:
          select:
            options:
              - Normal
              - Debug
    sequence:
      - if:
          - not:
              - condition: state
                entity_id: input_select.home_log_level
                state: "Off"
          - condition: template
            value_template: "{{ log_level|default(none, true) is none or log_level == states('input_select.home_log_level') }}"
        then:
          - if:
            - condition: template
              value_template: "{{ 'Slack' in states('input_text.home_log_platforms') }}"
            then:
              - service: script.post_slack_notification
                data:
                  message: "{{ message }}"
                  title: "{{ title|default(none, true) }}"
                  target: "{{ states('input_text.slack_home_log_channel_id') }}"
                continue_on_error: true
          - if:
            - condition: template
              value_template: "{{ 'Discord' in states('input_text.home_log_platforms') }}"
            then:
              - service: script.post_discord_notification
                data:
                  message: "{{ message }}"
                  title: "{{ title|default(none, true) }}"
                  target: "{{ states('input_text.discord_home_log_channel_id') }}"
                continue_on_error: true
    mode: queued
    max: 10
  send_direct_notification:
    alias: Send Direct Mesage To a Person
    icon: mdi:message-outline
    description: "Send a direct text message to adults in the house."
    fields:
      message:
        description: Message to post
        required: true
        selector:
          text:
            multiline: true
      title:
        description: Title to post
        selector:
          text:
      people:
        description:
          (optional)People to message using people entity in a comma separated list.
          If no one is specified, it will go to everyone.
        example: "['person.danny'] or ['person.danny','person.terina']"
        selector:
          target:
            entity:
              domain:
                - person
    variables:
      # Get people's preferred messaging platform
      slack: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people.entity_id, 'Slack') }}
      discord: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people.entity_id, 'Discord') }}
      mobile: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people.entity_id, 'Mobile') }}
      telegram: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people.entity_id, 'Telegram') }}
      whatsapp: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people.entity_id, 'WhatsApp') }}
    sequence:
      - service: script.send_to_home_log
        data:
          message: "{{ message }}"
          title: "{{ title|default(none, true) }}"
      # Check if notifications is turned on.
      - if:
          - condition: state
            entity_id: input_boolean.enable_direct_notifications
            state: "on"
        then:
          # Check anyone has set Slack for their direct messages
          - if:
              - condition: template
                value_template: "{{ slack|default('', true)|length > 0 }}"
            then:
              - service: script.post_slack_notification
                data:
                  message: "{{ message }}"
                  title: "{{ title|default(none, true) }}"
                  target: "{{ states('input_text.slack_direct_notification_channel_id') }}"
                  people: "{{ slack|join(' ')|replace('person.danny', '<@'~states('input_text.dannys_slack_id')~'>')
                  |replace('person.terina', '<@'~states('input_text.terinas_slack_id')~'>')
                  |replace('person.leo', '<@'~states('input_text.leos_slack_id')~'>') }}"
                continue_on_error: true
          - if:
              - condition: template
                value_template: "{{ discord|default('', true)|length > 0 }}"
            then:
              - service: script.post_discord_notification
                data:
                  message: "{{ message }}"
                  title: "{{ title|default(none, true) }}"
                  target: "{{ states('input_text.discord_direct_notification_channel_id') }}"
                  people: "{{ discord|join(' ')
                    |replace('person.danny', '<@'~states('input_text.dannys_discord_chat_id')~'>')
                    |replace('person.terina', '<@'~states('input_text.terinas_discord_chat_id')~'>')
                    |replace('person.leo', '<@'~states('input_text.leos_discord_chat_id')~'>')
                    |replace('person.ashlee', '<@'~states('input_text.ashlees_discord_chat_id')~'>') }}"
                continue_on_error: true
          - repeat:
              for_each: "{{ telegram|default([], true) }}"
              sequence:
                - service: script.post_telegram_direct_notification
                  data:
                    message: "{{ message }}"
                    title: "{{ title|default(none, true) }}"
                    target: "{{ states('input_text.telegram_direct_notification_channel_id') }}"
                    people: "{{ repeat.item|replace('person.danny', states('input_text.dannys_telegram_chat_id')) }}"
                  continue_on_error: true
          - repeat:
              for_each: "{{ mobile|default([], true) }}"
              sequence:
                - service: script.post_home_assistant_direct_notification
                  data:
                    message: "{{ message }}"
                    title: "{{ title|default(none, true) }}"
                    people: "{{ repeat.item }}"
                  continue_on_error: true
          - repeat:
              for_each: "{{ whatsapp|default([], true) }}"
              sequence:
                - service: script.post_whatsapp_direct_notification
                  data:
                    message: "{{ message }}"
                    people: "{{ repeat.item }}"
                  continue_on_error: true
    mode: queued
    max: 10
  send_direct_notification_with_url:
    alias: Send Direct Mesage To a Person With A Link
    icon: mdi:message-outline
    description: "Send a direct text message to people with a URL."
    fields:
      message:
        description: Message to post
        required: true
        selector:
          text:
            multiline: true
      title:
        description: Title to post
        selector:
          text:
      people:
        description:
          (optional)People to message using people entity in a comma separated list.
          If no one is specified, it will go to everyone.
        example: "['person.danny'] or ['person.danny','person.terina']"
        selector:
          target:
            entity:
              domain:
                - person
      url:
        description: (optional)Link to file.
        required: true
        selector:
          text:
            type: "url"
      url_type:
        description: The type of resource the URL referes to.
        example: image
        required: true
        selector:
          select:
            options:
              - audio
              - image
              - video
              - web
    variables:
      # Get people's preferred messaging platform
      slack: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people, 'Slack') }}
      discord: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people, 'Discord') }}
      mobile: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people, 'Mobile') }}
      telegram: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people, 'Telegram') }}
      whatsapp: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people, 'WhatsApp') }}
    sequence:
      - service: script.send_to_home_log
        data:
          message: "{{ message }}"
          title: "{{ title|default(none, true) }}"
      # Check if notifications is turned on.
      - if:
          - condition: state
            entity_id: input_boolean.enable_direct_notifications
            state: "on"
        then:
          # Check anyone has set Slack for their direct messages
          - if:
              - condition: template
                value_template: "{{ slack|default('', true)|length > 0 }}"
            then:
              - service: script.post_to_slack_with_url_attachment
                data:
                  message: "{{ message }}"
                  title: "{{ title|default(none, true) }}"
                  target: "{{ states('input_text.slack_direct_notification_channel_id') }}"
                  people: "{{ slack|join(' ')|replace('person.danny', states('input_text.dannys_slack_id'))
                  |replace('person.terina', states('input_text.terinas_slack_id'))
                  |replace('person.leo', states('input_text.leos_slack_id')) }}"
                continue_on_error: true
          - if:
              - condition: template
                value_template: "{{ discord|default('', true)|length > 0 }}"
            then:
              - service: script.post_discord_notification
                data:
                  message: >-
                    {{ message }}


                    {{ url }}
                  title: "{{ title|default(none, true) }}"
                  target: "{{ states('input_text.discord_direct_notification_channel_id') }}"
                  people: "{{ discord|join(' ')|replace('person.danny', '<@'~states('input_text.dannys_discord_chat_id')~'>')
                    |replace('person.terina', '<@'~states('input_text.terinas_discord_chat_id')~'>')
                    |replace('person.leo', '<@'~states('input_text.leos_discord_chat_id')~'>') }}"
                continue_on_error: true
          - repeat:
              for_each: "{{ telegram|default([], true) }}"
              sequence:
                - service: script.post_telegram_direct_notification
                  data:
                    message: >-
                      {{ message }}


                      {{ url }}
                    title: "{{ title|default(none, true) }}"
                    target: "{{ states('input_text.telegram_direct_notification_channel_id') }}"
                    people: "{{ repeat.item|replace('person.danny', states('input_text.dannys_telegram_chat_id')) }}"
                  continue_on_error: true
          - repeat:
              for_each: "{{ mobile|default([], true) }}"
              sequence:
                - service: script.post_home_assistant_direct_notification
                  data:
                    message: "{{ message }}"
                    title: "{{ title|default(none, true) }}"
                    people: "{{ repeat.item }}"
                    data:
                      image: "{{ url }}"
                  continue_on_error: true
          - repeat:
              for_each: "{{ whatsapp|default([], true) }}"
              sequence:
                - service: script.post_whatsapp_direct_notification
                  data:
                    message: >-
                      {{ message }}


                      {{ url }}
                    people: "{{ repeat.item }}"
                  continue_on_error: true
    mode: queued
    max: 10
  # Homelog
  send_home_log_with_local_attachments:
    alias: Send To Home Log With Local Attachments
    icon: mdi:message-image-outline
    description: "Post a message with a file attachment to the home_log channel."
    fields:
      message:
        description: Message to post
        required: true
        selector:
          text:
            multiline: true
      title:
        description: (optional)Title to the message posted.
        selector:
          text:
      filePath:
        description: File path to attach to post.
        required: true
        example: "/config/www/myImage.jpg"
        selector:
          text:
            type: "url"
      people:
        description:
          (optional)People to message using people entity in a comma separated list.
          If no one is specified, it will be posted to the home log.
        example: "['person.danny'] or ['person.danny','person.terina']"
        selector:
          target:
            entity:
              domain:
                - person
    variables:
      # Get people's preferred messaging platform
      slack: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people|default('', true)|length == 0 else people.entity_id, 'Slack') }}
      discord: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people|default('', true)|length == 0 else people.entity_id, 'Discord') }}
      mobile: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people|default('', true)|length == 0 else people.entity_id, 'Mobile') }}
      telegram: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people|default('', true)|length == 0 else people.entity_id, 'Telegram') }}
    sequence:
      - parallel:
          - if:
              - not:
                  - condition: state
                    entity_id: input_select.home_log_level
                    state: "Off"
            then:
              - parallel:
                  - service: script.post_to_slack_with_local_attachments
                    data:
                      message: "{{ message }}"
                      title: "{{ title|default(none, true) }}"
                      target: "{{ states('input_text.slack_home_log_channel_id') }}"
                      filePath: "{{ filePath }}"
                    continue_on_error: true
                  - service: script.post_to_discord_with_local_attachments
                    data:
                      message: "{{ message }}"
                      title: "{{ title|default(none, true) }}"
                      target: "{{ states('input_text.discord_home_log_channel_id') }}"
                      filePath: "{{ filePath }}"
                    continue_on_error: true
          # Check if people parameter has been set before checking which platform to send file attachments.
          - if:
              - condition: template
                value_template: "{{ people|default('', true)|length > 0 }}"
              - condition: state
                entity_id: input_boolean.enable_direct_notifications
                state: "on"
            then:
              - if:
                  - condition: template
                    value_template: "{{ slack|default('', true)|length > 0 }}"
                then:
                  - service: script.post_to_slack_with_local_attachments
                    data:
                      message: "{{ message }}"
                      title: "{{ title|default(none, true) }}"
                      target: "{{ states('input_text.slack_direct_notification_channel_id') }}"
                      people: "{{ slack|join(' ')|replace('person.danny', states('input_text.dannys_slack_id'))
                      |replace('person.terina', states('input_text.terinas_slack_id'))
                      |replace('person.leo', states('input_text.leos_slack_id')) }}"
                      filePath: "{{ filePath }}"
                    continue_on_error: true
              - if:
                  - condition: template
                    value_template: "{{ discord|default('', true)|length > 0 }}"
                then:
                  - service: script.post_to_discord_with_local_attachments
                    data:
                      message: "{{ message }}"
                      title: "{{ title|default(none, true) }}"
                      target: "{{ states('input_text.discord_direct_notification_channel_id') }}"
                      people: "{{ discord|join(' ')|replace('person.danny', '<@'~states('input_text.dannys_discord_chat_id')~'>')
                      |replace('person.terina', '<@'~states('input_text.terinas_discord_chat_id')~'>')
                      |replace('person.leo', '<@'~states('input_text.leos_discord_chat_id')~'>') }}"
                      filePath: "{{ filePath }}"
                    continue_on_error: true
              - repeat:
                  for_each: "{{ telegram|default([], true) }}"
                  sequence:
                    - service: script.post_to_discord_with_local_attachments
                      data:
                        message: "{{ message }}"
                        title: "{{ title|default(none, true) }}"
                        target: "{{ states('input_text.slack_direct_notification_channel_id') }}"
                        people: "{{ repeat.item|replace('person.danny', '<@'~states('input_text.dannys_telegram_chat_id'))~'>' }}"
                        filePath: "{{ filePath }}"
                      continue_on_error: true
    mode: queued
    max: 10
  send_home_log_with_url:
    alias: Send To Home Log With URL
    icon: mdi:message-bookmark-outline
    description: "Post a message with a URL to the home_log channel."
    fields:
      message:
        description: Message to post
        required: true
        selector:
          text:
            multiline: true
      title:
        description: (optional)Title to the message posted.
        selector:
          text:
      url:
        description: (optional)Link to file.
        required: true
        selector:
          text:
            type: "url"
    sequence:
      - if:
          - not:
              - condition: state
                entity_id: input_select.home_log_level
                state: "Off"
        then:
          - parallel:
              - service: script.post_to_slack_with_url_attachment
                data:
                  message: "{{ message }}"
                  title: "{{ title|default(none, true) }}"
                  target: "{{ states('input_text.slack_home_log_channel_id') }}"
                  url: "{{ url }}"
              - service: script.post_to_discord_with_url_attachment
                data:
                  message: "{{ message }}"
                  title: "{{ title|default(none, true) }}"
                  target: "{{ states('input_text.discord_home_log_channel_id') }}"
                  url: "{{ url }}"
    mode: queued
    max: 10
  send_actionable_notification_with_2_buttons:
    alias: Send Actionable Notification With 2 Buttons
    icon: mdi:message-draw
    fields:
      message:
        description: Message to post
        required: true
        selector:
          text:
            multiline: true
      title:
        description: (optional)Title to the message posted.
        selector:
          text:
      people:
        description: People to message.
        example: "['person.danny'] or ['person.danny','person.terina']"
        required: true
        selector:
          target:
            entity:
              domain:
                - person
      action1_title:
        description: First action text.
        required: true
        selector:
          text:
      action1_name:
        description: First vent name if action is selected.
        required: true
        selector:
          text:
      action2_title:
        description: Second action text.
        required: true
        selector:
          text:
      action2_name:
        description: Second event name if action is selected.
        required: true
        selector:
          text:
    variables:
      # Get people's preferred messaging platform
      mobile: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people, 'Mobile') }}
      telegram: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people, 'Telegram') }}
    sequence:
      - if:
          - condition: state
            entity_id: input_boolean.enable_direct_notifications
            state: "on"
        then:
          - repeat:
              for_each: "{{ telegram|default([], true) }}"
              sequence:
                - service: script.post_telegram_direct_notification
                  data:
                    message: "{{ message }}"
                    title: "{{ title|default(none, true) }}"
                    target: "{{ states('input_text.telegram_direct_notification_channel_id') }}"
                    people: "{{ repeat.item|replace('person.danny', states('input_text.dannys_telegram_chat_id')) }}"
          - repeat:
              # for_each: "{{ mobile|default([], true) }}"
              for_each: "{{ people.entity_id|default([], true) }}"
              sequence:
                - choose:
                    - conditions:
                      - condition: template
                        value_template: "{{ repeat.item == 'person.danny' }}"
                      sequence:
                        - service: notify.mobile_app_danny_s_phone
                          data:
                            message: "{{ message }}"
                            title: "{{ title|default('', true) }}"
                            data:
                              actions:
                                - title: "{{ action1_title }}"
                                  action: "{{ action1_name }}"
                                - title: "{{ action2_title }}"
                                  action: "{{ action2_name }}"
                    - conditions:
                      - condition: template
                        value_template: "{{ repeat.item == 'person.terina' }}"
                      sequence:
                        - service: notify.mobile_app_terina_s_phone
                          data:
                            message: "{{ message }}"
                            title: "{{ title|default('', true) }}"
                            data:
                              actions:
                                - title: "{{ action1_title }}"
                                  action: "{{ action1_name }}"
                                - title: "{{ action2_title }}"
                                  action: "{{ action2_name }}"
                    - conditions:
                      - condition: template
                        value_template: "{{ repeat.item == 'person.leo' }}"
                      sequence:
                        - service: notify.mobile_app_ipad_air_4th_generation_6730
                          data:
                            message: "{{ message }}"
                            title: "{{ title|default('', true) }}"
                            data:
                              actions:
                                - title: "{{ action1_title }}"
                                  action: "{{ action1_name }}"
                                - title: "{{ action2_title }}"
                                  action: "{{ action2_name }}"
    mode: queued
    max: 10
  send_actionable_notification_with_3_buttons:
    alias: Send Actionable Notification With 3 Buttons
    icon: mdi:message-draw
    fields:
      message:
        description: Message to post
        required: true
        selector:
          text:
            multiline: true
      title:
        description: (optional)Title to the message posted.
        selector:
          text:
      people:
        description: People to message.
        example: "['person.danny'] or ['person.danny','person.terina']"
        required: true
        selector:
          target:
            entity:
              domain:
                - person
      action1_title:
        description: First action text.
        selector:
          text:
      action1_name:
        description: First vent name if action is selected.
        required: true
        selector:
          text:
      action2_title:
        description: Second action text.
        required: true
        selector:
          text:
      action2_name:
        description: Second event name if action is selected.
        required: true
        selector:
          text:
      action3_title:
        description: Third action text.
        required: true
        selector:
          text:
      action3_name:
        description: Third event name if action is selected.
        required: true
        selector:
          text:
    variables:
      # Get people's preferred messaging platform
      mobile: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people, 'Mobile') }}
      telegram: >-
        {% from 'get_preferred_direct_message_platform.jinja' import preferred_direct_message_platform %}
        {{ preferred_direct_message_platform(states('input_text.direct_message_list') if people.entity_id|default('', true)|length == 0  else people, 'Telegram') }}
    sequence:
      - if:
          - condition: state
            entity_id: input_boolean.enable_direct_notifications
            state: "on"
        then:
          - repeat:
              for_each: "{{ telegram|default([], true) }}"
              sequence:
                - service: script.post_telegram_direct_notification
                  data:
                    message: "{{ message }}"
                    title: "{{ title|default(none, true) }}"
                    target: "{{ states('input_text.telegram_direct_notification_channel_id') }}"
                    people: "{{ repeat.item|replace('person.danny', states('input_text.dannys_telegram_chat_id')) }}"
          - repeat:
              for_each: "{{ mobile|default([], true) }}"
              sequence:
                - choose:
                    - conditions:
                      - condition: template
                        value_template: "{{ repeat.item == 'person.danny' }}"
                      sequence:
                        - service: notify.mobile_app_danny_s_phone
                          data:
                            message: "{{ message }}"
                            title: "{{ title|default('', true) }}"
                            data:
                              actions:
                                - title: "{{ action1_title }}"
                                  action: "{{ action1_name }}"
                                - title: "{{ action2_title }}"
                                  action: "{{ action2_name }}"
                                - title: "{{ action3_title }}"
                                  action: "{{ action3_name }}"
                    - conditions:
                      - condition: template
                        value_template: "{{ repeat.item == 'person.terina' }}"
                      sequence:
                        - service: notify.mobile_app_terina_s_phone
                          data:
                            message: "{{ message }}"
                            title: "{{ title|default('', true) }}"
                            data:
                              actions:
                                - title: "{{ action1_title }}"
                                  action: "{{ action1_name }}"
                                - title: "{{ action2_title }}"
                                  action: "{{ action2_name }}"
                                - title: "{{ action3_title }}"
                                  action: "{{ action3_name }}"
                    - conditions:
                      - condition: template
                        value_template: "{{ repeat.item == 'person.leo' }}"
                      sequence:
                        - service: notify.mobile_app_ipad_air_4th_generation_6730
                          data:
                            message: "{{ message }}"
                            title: "{{ title|default('', true) }}"
                            data:
                              actions:
                                - title: "{{ action1_title }}"
                                  action: "{{ action1_name }}"
                                - title: "{{ action2_title }}"
                                  action: "{{ action2_name }}"
                                - title: "{{ action3_title }}"
                                  action: "{{ action3_name }}"
          - service: script.send_actionable_notification_with_3_buttons
            data:
              message: "{{ message }}"
              title: "{{ title|default(none, true) }}"
              people: "{{ people }}"
              data:
                action1_title: "{{ action1_title }}"
                action1_name: "{{ action1_name }}"
                action2_title: "{{ action2_title }}"
                action2_name: "{{ action2_name }}"
                action3_title: "{{ action3_title }}"
                action3_name: "{{ action3_name }}"
    mode: single
  send_to_computers:
    alias: "Send notification to computers"
    icon: mdi:message-draw
    description: >-
      Send notification to computeres.
    fields:
      message:
        description: Message to post
        required: true
        selector:
          text:
            multiline: true
      title:
        description: (optional)Title to the message posted.
        selector:
          text:
      filePath:
        description: File path to attach to post.
        selector:
          text:
      duration:
        description: >
          How long to display the notification in seconds. It will default to 10 seconds if one has not been set.
        default: 10
        selector:
          number:
            min: 1
            max: 300
            step: 1
            unit_of_measurement: "sec"
            mode: "box"
    sequence:
      - if:
          - condition: state
            entity_id: input_boolean.enable_computer_notifications
            state: "on"
        then:
          - parallel:
              - if:
                  - not:
                    - condition: state
                      entity_id: media_player.jd
                      state: "unavailable"
                then:
                  - service: script.post_to_hass_agent
                    data:
                      message: "{{ message }}"
                      title: "{{ title }}"
                      filePath: "{{ filePath }}"
                      device: notify.jd
                      duration: "{{ duration }}"
              - if:
                  - not:
                    - condition: state
                      entity_id: media_player.sam
                      state: "unavailable"
                then:
                  - service: script.post_to_hass_agent
                    data:
                      message: "{{ message }}"
                      title: "{{ title }}"
                      filePath: "{{ filePath }}"
                      device: notify.sam
                      duration: "{{ duration }}"
    mode: queued
    max: 10

  # HASS Agent
  post_to_hass_agent:
    alias: "Post To HASS Agent File Attachments"
    icon: mdi:message-image
    description: >-
      Post notification to a desktop client.
    fields:
      message:
        description: Message to post
        required: true
        selector:
          text:
            multiline: true
      title:
        description: (optional)Title to the message posted.
        selector:
          text:
      filePath:
        description: File path to attach to post.
        selector:
          text:
      device:
        description: HASS Agent notify service.
        example: notify.sam
        required: true
        selector:
          target:
            entity:
              domain:
                - notify
      duration:
        description: >
          How long to display the notification in seconds. It will default to 10 seconds if one has not been set.
        default: 10
        selector:
          number:
            min: 1
            max: 300
            step: 1
            unit_of_measurement: "sec"
            mode: "box"
    sequence:
        - choose:
        # Check a title was set
          - conditions:
              - condition: template
                value_template: "{{ filePath is not none }}"
              - condition: template
                value_template: "{{ title is not none }}"
            sequence:
              - service: "{{ device }}"
                data:
                  message: "{{ message }}"
                  title: "{{ title }}"
                  data:
                    image: "{{ filePath }}"
                    duration: "{{ duration|default(10, true) }}"
          - conditions:
              - condition: template
                value_template: "{{ filePath is not none }}"
              - condition: template
                value_template: "{{ title is none }}"
            sequence:
              - service: "{{ device }}"
                data:
                  message: "{{ message }}"
                  data:
                    image: "{{ filePath }}"
                    duration: "{{ duration|default(10, true) }}"
          - conditions:
              - condition: template
                value_template: "{{ title is not none }}"
            sequence:
              - service: "{{ device }}"
                data:
                  message: "{{ message }}"
                  data:
                    image: "{{ filePath }}"
                    duration: "{{ duration|default(10, true) }}"
          default:
            - service: "{{ device }}"
              data:
                message: "{{ message }}"
                data:
                  duration: "{{ duration|default(10, true) }}"
  announce_delayed_notifications:
    alias: "Announce Delayed Notifications"
    fields:
      test:
        default: false
      people:
        description:
          (optional)People to message using people entity in a comma separated list.
          If no one is specified, it will go to everyone.
        example: "['person.danny'] or ['person.danny','person.terina']"
        selector:
          target:
            entity:
              domain:
                - person
    sequence:
      - service: todo.get_items
        target:
          entity_id:
            - todo.shared_notifications
            - todo.dannys_notifications
        data:
          status: needs_action
        response_variable: todo_list
      - variables:
          shared: >-
            {%- for item in todo_list['todo.shared_notifications']['items'] %}
              * {{ item.summary }}

        
            {%- endfor %}
      - choose:
        - alias: Danny
          conditions:
            - condition: template
              value_template: "{{ people.entity_id|default('', true) in 'person.danny' }}"
            - or:
                - condition: template
                  value_template: "{{ todo_list['todo.dannys_notifications']['items']|default([], true)|count > 0 }}"
                - condition: template
                  value_template: "{{ todo_list['todo.shared_notifications']['items']|default([], true)|count > 0 }}"
          sequence:
            - alias: Deliver announcement
              service: script.send_direct_notification
              data:
                message: >-
                  Please be aware of the follow:
                  {%- for item in todo_list['todo.dannys_notifications']['items'] %}
                    * {{ item.summary }}

                    
                  {% endfor %}

                  Shared:

                  {{ shared }}
                title: "Delayed Annoucements"
                people:
                  entity_id:
                    - person.danny
            - if:
                - condition: template
                  value_template: "{{ test == false }}"
              then:
                - alias: Mark stored announcements as complete
                  repeat:
                    for_each: "{{ todo_list['todo.dannys_notifications']['items'] }}"
                    sequence:
                      - service: todo.update_item
                        target:
                          entity_id:
                            - todo.dannys_notifications
                        data:
                          item: "{{ repeat.item.summary }}"
                          status: completed
        default: []
